// Example osrv CLI config file.
// The CLI currently uses best-effort parsing for port/hostname/protocol keys.
const Map<String, Object> osrvConfig = <String, Object>{
  'port': 3000,
  'hostname': '0.0.0.0',
  'protocol': 'http',
};
