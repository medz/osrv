# osrv_example

Minimal non-publishable pub package that depends on `osrv` via a local path.

## Run

```bash
dart pub get
dart run osrv serve
```

## Build

```bash
dart pub get
dart run osrv build
```
